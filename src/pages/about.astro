---
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import "../styles/global.css";

const pageTitle = "私について";

const identity = {
  firstName: "サラ",
  country: "カナダ",
  occupation: "技術ライター",
  hobbies: ["写真", "バードウォッチング", "野球"],
};

const skiils = [
  "HTML",
  "CSS",
  "JavaScript",
  "React",
  "Astro",
  "ドキュメントの執筆",
];

const skillColor = "navy";
const fontWeight = "bold";
const textCase = "Uppercase";
---

<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <style define:vars={{ skillColor, fontWeight, textCase }}>
      h1 {
        color: purple;
        font-size: 4rem;
      }
      .skill {
        color: var(--skillColor);
        font-weight: var(--fontWeight);
        text-transform: var(--textCase);
      }
    </style>
  </head>
  <body>
    <Header />
    <h1>{pageTitle}</h1>
    <h2>... そして私の新しいAstroサイトについて</h2>

    <p>
      私はAstroの入門チュートリアルを勧めています。これは私のウェブサイトの2番めのページです。
    </p>
    <p>
      このサイトはチュートリアルを進めるに連れて更新されていきます。定期的にチェックしてください
    </p>

    <p>以下はたわしに関するいくつかの事実です</p>
    <ul>
      <li>私の名前は{identity.firstName}です。</li>
      <li>
        {identity.country}に住んでおり、{
          identity.occupation
        }として働いています。
      </li>
      {
        identity.hobbies.length >= 2 && (
          <li>
            私の2つの趣味は{identity.hobbies[0]}と{identity.hobbies[1]}です。
          </li>
        )
      }
    </ul>
    <p>私のスキルは以下のとおりです</p>
    <ul>
      {skiils.map((skill) => <li class="skill">{skill}</li>)}
    </ul>
    <Footer />
  </body>
</html>
